"""IDA Suite Runner package

This package contains utilities for discovering and executing IDA case suites
in parallel. It was previously called `test_4_ida_runner` before being renamed
during code cleanup and reorganization. Import paths should now use `ida_suite_runner`.
"""